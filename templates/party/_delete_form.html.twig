<form method="post" action="{{ path('app_party_delete', {'id': party.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ party.id) }}">
    <button class="btn">Delete</button>
</form>
